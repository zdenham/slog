{
  "name": "slog",
  "module": "src/cli.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run src/cli.ts",
    "build": "bun build --compile src/cli.ts --outfile slog",
    "test": "bun test",
    "test:bin": "bun run build && SLOG_BIN=./slog bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11",
    "typescript": "^6.0.2"
  },
  "dependencies": {
    "ulid": "^3.0.2"
  }
}
